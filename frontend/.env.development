# Vite dev-server proxies /api to this URL — no CORS needed in development.
# Change this if your Laravel backend runs on a different port or Laragon domain.
VITE_BACKEND_URL=https://claude-upskilling-backend-production.up.railway.app
